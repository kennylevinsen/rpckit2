{{- if ne (len .Fields) 0 -}}
type {{ required .Prefix }}Proto{{ required .ProtoName | capitalize }}Method{{ required .Name | capitalize }} struct {
{{- range required .Fields }}
    {{ required .Name | capitalize }} {{ required .T.GoType }} `json:{{ required .Name | camelize | doublequote }}`
{{- end }}
}
{{- end }}
