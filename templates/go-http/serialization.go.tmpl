{{- $prefix := .Prefix }}
type {{ .Prefix }}Proto{{ capitalize .ProtoName }}Method{{ capitalize .Name }} struct {
{{- range .Fields }}
    {{ capitalize .Name }} {{ .T.GoType }} `json:{{ doublequote .Name }}`
{{- end }}
}
